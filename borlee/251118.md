**닉네임**: borlee

**문제 플랫폼**: 프로그래머스

**문제 이름 또는 번호**: 배열 조각하기

**문제 링크**: https://school.programmers.co.kr/learn/courses/30/lessons/181893

**오늘의 메모(선택)**: 
코딩 기초 트레이닝 
국어 문제...?
start > end 인 경우는 고려하지 않아도 통과는 된다
```
제한사항

query의 각 원소는 0보다 크거나 같고 남아있는 arr의 길이 보다 작습니다.
```
왜 고려하지 않아도 되는지 제한사항은 찾았는데, 미묘하다.


**코드(선택)**
```c
#include <stdio.h>
#include <stdbool.h>
#include <stdlib.h>
#include <string.h>

// arr_len은 배열 arr의 길이입니다.
// query_len은 배열 query의 길이입니다.
int* solution(int arr[], size_t arr_len, int query[], size_t query_len) {
    // return 값은 malloc 등 동적 할당을 사용해주세요. 할당 길이는 상황에 맞게 변경해주세요.
    int start = 0;
    int end = arr_len - 1;
    int result_len = 0;
    int *result;
    
    for (size_t i = 0; i < query_len; i++)
    {
        int idx = query[i];
        if (i % 2 == 0)
            end = start + idx;
        else
            start = start + idx;
    }
    if (start <= end)
        result_len = end - start + 1;
    else
        result_len = 0;
    // if (result_len == 0)
    // {
    //     result =  malloc(1);
    //     return result;
    // }
    result = malloc(sizeof(int) * result_len);
    if (!result)
        return NULL;
    memcpy(result, arr + start, sizeof(*result) * result_len);
    return result;
}
```