닉네임: eonoh

문제 플랫폼: SWEA

문제 이름 또는 번호: 1767 / 프로세서 연결하기

문제 링크: https://swexpertacademy.com/main/code/problem/problemDetail.do?contestProbId=AV4suNtaXFEDFAUf

오늘의 메모(선택): 흐ㅁ...

코드(선택): java

```cpp

#include <iostream>
#include <limits.h>
#include <algorithm>
using namespace std;

int cells[12][12];
int n;
int dy[4] = { -1, 1, 0, 0 };
int dx[4] = { 0, 0, -1, 1 };

int search(int ny, int nx, int option) {
	int cnt[4], minLength;
	memset(cnt, 0, sizeof(cnt));

	// 상
	for (int y = 0; y < ny; y++) {
		if (cells[y][nx] == 1) {
			cnt[0] = INT_MAX;
			break;
		}
		if (option == 0) cells[y][nx] = 2;
		cnt[0]++;
	}
	// 하
	for (int y = ny; y < n; y++) {
		if (cells[y][nx] == 1) {
			cnt[1] = INT_MAX;
			break;
		}
		if (option == 1) cells[y][nx] = 2;
		cnt[1]++;
	}
	// 좌
	for (int x = 0; x < nx; x++) {
		if (cells[ny][x] == 1) {
			cnt[2] = INT_MAX;
			break;
		}
		if (option == 3) cells[ny][x] = 2;
		cnt[2]++;
	}
	// 우
	for (int x = nx; x < n; x++) {
		if (cells[ny][x] == 1) {
			cnt[3] = INT_MAX;
			break;
		}
		if (option == 3) cells[ny][x] = 2;
		cnt[3]++;
	}
	int minLength = min(min(min(cnt[0], cnt[1]), cnt[2]), cnt[3]);
	if (minLength == INT_MAX) return 0;
	return minLength;
}

int solution() {
	for (int y = 0; y < n; y++) {
		for (int x = 0; x < n; x++) {
			if (y == 0 || x == 0 || y == (n - 1) || x == (n - 1)) continue;
			if (cells[y][x] == 1) {

			}
		}
	}
}

int main()
{
	ios::sync_with_stdio(false), cin.tie(NULL);

	int t;
	
	cin >> t;
	for (int tc = 1; tc <= t; tc++) {
		cin >> n;
		for (int y = 0; y < n; y++) {
			for (int x = 0; x < n; x++) {
				cin >> cells[y][x];
			}
		}
	}
}
```
