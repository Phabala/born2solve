**닉네임**: taebkim

**문제 플랫폼**: 백준

**문제 이름 또는 번호**: 16433 / 주디와 당근농장

**문제 링크**: https://www.acmicpc.net/problem/16433

**오늘의 메모(선택)**:

**코드(선택)**:

```c++
#include <algorithm>
#include <cmath>
#include <cstring>
#include <iomanip>
#include <iostream>
#include <map>
#include <numeric>
#include <queue>
#include <set>
#include <sstream>
#include <stack>
#include <stdexcept>
#include <string>
#include <unordered_set>
#include <utility>
#include <vector>

#define TAEBKIM                                                                \
    std::ios::sync_with_stdio(false);                                          \
    std::cin.tie(NULL);                                                        \
    std::cout.tie(NULL);
;

using namespace std;

int main() {
    TAEBKIM;

    int n, r, c;
    cin >> n >> r >> c;

    for (int i = 1; i <= n; ++i) {
        for (int j = 1; j <= n; ++j) {
            cout << ((i % 2) ^ (j % 2) == (r % 2) ^ (c % 2) ? 'v' : '.');
        }
        cout << "\n";
    }

    return 0;
}
```
