**닉네임**: taebkim

**문제 플랫폼**: 백준

**문제 이름 또는 번호**: 12847 / 꿀 아르바이트

**문제 링크**: https://www.acmicpc.net/problem/12847

**오늘의 메모(선택)**:

**코드(선택)**:

```c++
#include <algorithm>
#include <cmath>
#include <iomanip>
#include <iostream>
#include <map>
#include <numeric>
#include <queue>
#include <set>
#include <stack>
#include <stdexcept>
#include <string>
#include <unordered_set>
#include <utility>
#include <vector>

int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);

    int n, m;
    std::cin >> n >> m;

    std::vector<int> money;
    long long int sum = 0;
    for (int i = 0; i < n; i++) {
        int num;
        std::cin >> num;

        money.push_back(num);
        if (i < m) {
            sum += num;
        }
    }

    int start = 0;
    int end = m - 1;
    long long int max = sum;
    while (true) {
        sum -= money[start];
        start++;
        end++;
        if (end >= n) {
            break;
        } else {
            sum += money[end];
        }

        if (max < sum) {
            max = sum;
        }
    }
    std::cout << max << "\n";
    return 0;
}

```